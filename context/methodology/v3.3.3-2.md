# v3.3.3-2 — boundary-start prototype f0 + posterior-odds restart

> **⚠ DEPRECATED (2026-05-10)**
>
> iter1 sanity 결과: T1var 폭증 (47.5→72.0, segmentation 결정 분산 증가). "별로면 다시 구현" 규칙 적용 → **채택 X, iter2 v3.3.3-3 로 대체**. v3.3.3-3 도 segmentation 안정만 성공 → **iter3 [v3.3.3-4](v3.3.3-4.md) 로 최종 대체**.
>
> 본 파일은 **재현성 / 향후 ablation 비교용 reference** 로 보존. 코드 (`src/hi_ontop/sem_core_v333_2.py`) 는 그대로 유지.
>
> 폐기 사유 자세히는 [`context/06-decision-log.md` 2026-05-10 v3.3.3-2 entry](../06-decision-log.md) 참조.


CLI method: `hi-ontop-full-v3.3.3-2`. Segmenter class: `HiOnTopSegmenterV333_2` (구현 2026-05-10).

## 한 줄

v3.3.3 의 same-label restart 분기를 강화. f0 centroid 단일 대신 **boundary-start prototype top-M list** 를 유지하고, hard `restart_pe_threshold` 대신 **posterior odds** $p_{rst}>\rho_{rst}$ 로 재정식화.

## 동기

v3.3.3 first sweep (2026-05-09) 에서 default HP `restart_pe_threshold=0.5, restart_margin=0.0` 가 너무 보수적이라 restart 분기가 거의 발화 안 함 (n_topics / T1μ 가 v3.3.2 와 사실상 동일). v3.3.3.md 분석 가설 셋:

1. `restart_pe_threshold=0.5` 가 너무 엄격 (repeat PE ≤ 0.5 면 restart 안 봄)
2. cosine surrogate 환경에서 f0 단일 centroid 가 RNN repeat prediction 을 거의 못 이김
3. `restart_margin=0` 도 noise 에 묻힘

v3.3.3-2 는 (1)(3) 을 posterior softmax 로, (2) 를 prototype top-M `max` 로 보정한다.

## 새 메커니즘

### Prototype f0

각 topic $k$ 가 boundary-start embedding 의 top-M unit-norm prototype list $P_k$ 를 유지 (default `f0_proto_max=4`). 새 boundary-start sample $s$ 가 들어오면:

- `len(P_k) < M`: append.
- `len(P_k) == M`: 가장 cosine 유사한 prototype 과 평균 후 재정규화 (mass conservation).

$$f0\_score(k, s) = \max_{p \in P_k} \cos(p, s)$$

cold-start (`f0_count < f0_min_starts`) 에는 v3.3.3 와 동일하게 topic centroid `mu` fallback.

### Posterior-odds restart

기존 $S^{rep}_k, S^{rst}_k$ 식은 v3.3.3 와 동일. 단 `k = k_{prev}` 에서 `S = max(rep, rst)` 후의 boundary 판정을 hard PE gate 대신 softmax posterior 로:

$$p_{rst} = \frac{\exp S^{rst}_{k_{prev}}}{\exp S^{rst}_{k_{prev}} + \exp S^{rep}_{k_{prev}}}$$

$$boundary_{same} = (p_{rst} > \rho_{rst})$$

default `restart_p_threshold=0.35` (낮춰 restart 활성화 의도). `restart_pe_min` (default `0.0`) 은 옵션 floor.

## 새 HP

| 이름 | default | 의미 |
|---|---:|---|
| `f0_proto_max` | 4 | topic 별 prototype 최대 수 |
| `restart_p_threshold` | 0.35 | same-label restart posterior 임계 |
| `restart_pe_min` | 0.0 | repeat PE 가 이 값 이상일 때만 restart 후보 (옵션 floor) |
| `f0_min_starts` | 2 | f0 신뢰 전 최소 boundary-start sample 수 (v3.3.3 상속) |
| `f0_tau` | None | None → tau 와 동일 (v3.3.3 상속) |

학습 추가 없음. f0 prototype 갱신은 cosine 평균 + L2 norm 만 — RNN train_steps 그대로, latency 동일.

## Cross-cutting HP (sweep 시 함께 고려)

- **Segmenter base** (v3.3.3 와 동일): `alpha`, `lmda`, `tau`, `cos_threshold`, `beta`, `pe_threshold`, `rnn_train_steps`, `rnn_hidden_dim`, `rnn_lr`, `rnn_max_context`, `rnn_min_history`.

## SEM 계승 정당화

- prototype top-M f0 는 **새 메커니즘**. SEM2 는 단일 f0 centroid (`update_f0(x_curr)`).
  1. **SEM 에 왜 없는가**: SEM2 자극은 controlled VAE 영상이라 event-시작 패턴이 단봉 분포. 대화 topic 은 같은 label 안에서도 재방문 시작 패턴이 다양하므로 multi-prototype 이 필요할 수 있음.
  2. **충돌**: SEM2 `new_token()` 의 정신은 살아 있음 (label 유지 + episode 분리). prototype list 가 그 token 의 다양한 시작 자세를 표현. sCRP / Bayes / local MAP 와 직교.
  3. **decision-log entry**: 2026-05-10 v3.3.3-2 첫 sanity.

- posterior odds 는 SEM2 `restart > repeat` 조건의 cosine-환경 재해석. SEM2 는 Gaussian likelihood 직접 비교; posterior softmax 는 그 비교의 numerically stable 형태이며 cosine surrogate 에서도 의미 보존.

## 2026-05-10 first sanity sweep (limit 200 stratified)

`outputs/experiments/2026-05-10_v33x_2_iter1/` (3 runs, default HP).

| 지표 | v3.3.3 (3 runs) | v3.3.3-2 (3 runs) | Δ |
|---|---:|---:|---:|
| T1μ | 25.7 | 26.3 | +0.6 |
| T1var | 47.5 ± 10.7 | **72.0 ± 48.5** | **+24.5 분산 폭증** |
| n_topics | 9.88 | 9.81 | ≈ |

**판정**: posterior threshold 0.35 가 너무 낮아 same-label restart 가 과다 발화 → segmentation 결정 분산이 커짐. T1var 47.5→72.0 (4.5x std) 가 그 직접 신호. n_topics 는 거의 동일하나 boundary 위치의 run-to-run 변동이 폭증.

## 판정 ("다시 구현" 규칙)

iter1 sanity 결과: T1var 폭증 (segmentation 결정 불안정) → **재구현 (iter2 v3.3.3-3)**.

## iter2 재설계 방향 (v3.3.3-3, 진행 예정)

codex 재설계 (`agentId: a4f3e7b1468a14b3f`) 권장:

1. **restart 보수화**: `restart_p_threshold=0.5` 로 상향 + `restart_prob_margin=0.15` 추가 + `episode_min_span=4` (직전 restart 후 최소 turn 간격).
2. **prototype softening**: `top-M max` → `(1-γ) cos(s, mean) + γ logmeanexp(cos(s, p))` 혼합 (`f0_proto_weight=0.25`).

이 둘이 T1var 안정 (segmentation 결정 분산 축소) 확보 의도.

## 알려진 한계

1. **restart 과다 발화 원인 미특정**: 가설 (posterior threshold 낮음, prototype dilution) 은 설계 직관. 데이터 검증 안 됨. iter2 에서 restart 발화 횟수 logging + `restart_p_threshold ∈ {0.35, 0.5, 0.7}` ablation 으로 격리 검증 필요.
2. **T1var 폭증 (47.5→72.0)**: RNN init noise 가 prototype f0 + posterior odds 에 증폭됨. iter2 설계 (hysteresis + minimum-span guard) 가 이를 흡수해야.
3. **prototype top-M `max`** 의 seed 민감도: 가장 가까운 prototype 1 개로만 평가 → init randomness 가 직접 score 에 흘러감. softening (logmeanexp) 으로 완화 예정.

## 변경 이력

- 2026-05-10: 초안 + 첫 sanity sweep + iter2 재설계 권장 추가 (codex thread `019e10f0-...`).

decision-log: `context/06-decision-log.md` 2026-05-10 v3.3.3-2 / v3.3.4-2 iter1 entry.
