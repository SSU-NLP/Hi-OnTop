# v2 — segmentation 변경 없음 (memory-pipeline only, pivot 에서 제거)

## 한 줄

**Segmenter 자체는 v1 과 동일** (`HiOnTopSegmenter`, Gaussian likelihood, raw sticky-CRP). v2 가 v1 에 더한 변경은 segmentation 알고리즘이 아니라 전적으로 memory-pipeline 인프라 수준이었고, 2026-05-17 standalone topic segmenter 전환(pivot) 에서 제거됨.

## Score 식 / Topic state / Update rule / SEM 계승

→ [v1.md](v1.md) 와 동일. v2 는 알고리즘 수준에서 v1 segmenter 를 그대로 재사용한다.

## 후속

- v3.x 라인은 segmenter 만 교체하며 v1 의 algorithm-level 정의를 상속한다.

## 변경 이력

- v2 의 segmentation-algorithm 변경은 없음. memory-pipeline 변경분은 pivot 에서 제거.
